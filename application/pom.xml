<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>net.unikernel.bummel</groupId>
        <artifactId>BUMMEL-parent</artifactId>
        <version>0.2.2</version>
    </parent>

    <artifactId>BUMMEL-app</artifactId>
    <packaging>nbm-application</packaging>

    <name>BUMMEL-app</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <netbeans.run.params.ide/>
        <netbeans.run.params>${netbeans.run.params.ide}</netbeans.run.params>
        <all.clusters>${project.build.directory}/${brandingToken}</all.clusters>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.netbeans.cluster</groupId>
            <artifactId>platform</artifactId>
            <version>RELEASE72</version>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>net.unikernel.bummel</groupId>
            <artifactId>BUMMEL-branding</artifactId>
            <version>0.2.2</version>
        </dependency>
        <dependency>
            <groupId>org.netbeans.api</groupId>
            <artifactId>org-netbeans-modules-nbjunit</artifactId>
            <version>RELEASE72</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>net.unikernel.bummel</groupId>
            <artifactId>project_model</artifactId>
            <version>0.2.2</version>
        </dependency>
        <dependency>
            <groupId>net.unikernel.bummel</groupId>
            <artifactId>logic_elements</artifactId>
            <version>0.2.2</version>
        </dependency>
        <dependency>
            <groupId>net.unikernel.bummel</groupId>
            <artifactId>advanced_logic_elements</artifactId>
            <version>0.2.2</version>
        </dependency>
        <dependency>
            <groupId>net.unikernel.bummel</groupId>
            <artifactId>latches</artifactId>
            <version>0.2.2</version>
        </dependency>
        <dependency>
            <groupId>net.unikernel.bummel</groupId>
            <artifactId>visual_editor</artifactId>
            <version>0.2.2</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>nbm-maven-plugin</artifactId>
            </plugin>
            <!-- Permits NbModuleSuite to be run in integration-test phase: -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.10</version>
                <configuration>
                    <systemPropertyVariables>
                        <all.clusters>${all.clusters}</all.clusters>
                        <branding.token>${brandingToken}</branding.token>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>deployment</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>nbm-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>extra</id>
                                <goals>
                                    <goal>autoupdate</goal>
                                    <goal>webstart-app</goal>
                                    <goal>build-installers</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
